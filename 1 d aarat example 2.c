#include<stdio.h>
int main()
{
	int sub[8],i,sum=0,avg;
	printf("\nEnter 8 elements:");
	for(i=0;i<8;i++)
	{
		scanf("%d",&sub[i]);
		sum=sum+sub[i];
		avg=sum/8;
	}
	printf("\nTotal subjects are:");
	for(i=0;i<8;i++)n
	{
		printf("\n%d",sum);
	}
	printf("\nAvg is:%d",avg);
	getch();
}
